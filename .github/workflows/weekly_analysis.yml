name: 週次フォロワー取得・ダッシュボード更新

on:
  schedule:
    # 毎週日曜21:00 JST = 12:00 UTC
    - cron: "0 12 * * 0"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Python セットアップ
        uses: actions/setup-python@v5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 依存パッケージをインストール
        run: pip install -r requirements.txt

      - name: 自アカウント統計を自動取得（Nitter）
        run: python scripts/fetch_own_stats.py

      - name: Nitterでフォロワー数・いいね数を取得
        run: python scripts/fetch_own_stats.py

      - name: HTMLダッシュボードを生成
        run: python scripts/generate_dashboard.py

      - name: ダッシュボードをコミット・プッシュ
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add docs/index.html
          git diff --staged --quiet || git commit -m "週次ダッシュボード更新 $(date +'%Y/%m/%d')"
          git push
